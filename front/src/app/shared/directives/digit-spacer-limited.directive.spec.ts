import { DigitSpacerLimitedDirective } from './digit-spacer-limited.directive';

describe('DigitSpacerLimitedDirective', () => {
  it('should create an instance', () => {
    const directive = new DigitSpacerLimitedDirective();
    expect(directive).toBeTruthy();
  });
});
