import { AutoTooltipDirective } from './auto-tooltip.directive';

describe('AutoTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
